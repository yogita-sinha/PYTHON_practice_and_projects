#collection or set is a collection which is unordered,
#unchangeable, and unindexed.
#In Python, sets are defined using curly braces {} 
#or the set() function.


collection = {1 , 2, 3 , 4  , "hello" , 1.5, (1,2,3) }
print (collection)
print(type(collection))
print(len(collection))
